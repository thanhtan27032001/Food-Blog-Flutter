part of 'search_recipe_controller.dart';

class SearchRecipePage extends GetView<SearchRecipeController> {

  @override
  final controller = Get.put(SearchRecipeController());

  SearchRecipePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}